package views
{
	import uk.co.jakerigby.sparkrl.framework.ui.components.ViewBase;
	
	/**
	 * The view is essentially the view model - re-presenting backend model information for the rendering script in the skin - often we only need to type it and maybe inject a few backend models
	 */
	public class Hello extends ViewBase
	{
	}
}